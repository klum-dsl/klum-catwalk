{
  "schemaVersion": 1,
  "releaseLine": "4.0.x",
  "retainedPublicPin": "4.0.1",
  "fixture": {
    "path": ":direct-schema",
    "topology": "one direct Schema consumer",
    "revision": "public 4.0.1 baseline promotion"
  },
  "compatibilityCommitment": "The retained direct Schema contract is revalidated against the exact released 4.0.1 public coordinates and its AnnoDocimal 1.0.1 release-line graph.",
  "taskGraph": [
    "clean",
    ":direct-schema:createKlumDslSourceMirrors",
    ":direct-schema:javadoc",
    ":direct-schema:check",
    "verifyResolvedConsumerEvidence"
  ],
  "expectations": {
    "normalSchemaPluginResolution": true,
    "nestedExternalSignatureProjection": true,
    "legacyParagraphAndTagJavadoc": true,
    "completedModelTest": true,
    "mirrorIsNotBuildOrPublicationInput": true,
    "historicalTestSupportIsNotRequired": true
  },
  "candidateRebinding": {
    "eligible": true,
    "status": "deferred to SC-2",
    "rule": "Bind this unchanged fixture contract only to one complete staged candidate product, including marker and implementation."
  },
  "knownLimitations": [
    "Future candidate rebinding remains an SC-2 gate and must bind one complete staged product.",
    "This baseline is not a remote bootstrap decision or a workflow/release gate."
  ]
}
