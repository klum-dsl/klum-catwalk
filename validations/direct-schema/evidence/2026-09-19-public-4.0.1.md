# Public 4.0.1 baseline validation

Date: 2026-09-19

Mode: `public-release`

Fresh Gradle user home: `/private/tmp/catwalk-public-401-final.IgrsWV`

JDK: 25.0.4

Gradle: 8.14.4

The run used only Maven Central and the Gradle Plugin Portal as declared by the
isolated consumer. No KlumAST or AnnoDocimal source checkout, composite build,
project/file dependency, `mavenLocal()` repository, candidate coordinate, or
local fallback was configured.

## Passing clean public-coordinate graph

```text
GRADLE_USER_HOME=/private/tmp/catwalk-public-401-final.IgrsWV \
  ./gradlew verifyDirectPublicBaseline --console=plain
```

Result: **passed**. All ten scheduled tasks executed from `clean`, including
`createKlumDslSourceMirrors`, `javadoc`, the completed-model Spock test,
`check`, and `verifyResolvedConsumerEvidence`. Javadoc completed with 22
non-fatal missing-comment/tag warnings in generated sources. The test report
records one test, zero failures, and zero errors.

## Resolved release evidence

The generated evidence binds these public 4.0.1 coordinates:

- Schema plugin marker
  `com.blackbuild.klum-ast-schema:com.blackbuild.klum-ast-schema.gradle.plugin:4.0.1`
- plugin implementation
  `com.blackbuild.klum.ast:klum-ast-gradle-plugin:4.0.1`
- BOM `com.blackbuild.klum.ast:klum-ast-bom:4.0.1`
- product modules `klum-ast-annotations`, `klum-ast`, `klum-ast-runtime`,
  `klum-ast-jackson`, and `klum-ast-bean-validation`, all at 4.0.1

The resolved product graph contains AnnoDocimal 1.0.1 `annotations`, `ast`,
`global-ast`, and `gradle-plugin`. The Schema compile graph contains the
intended AnnoDocimal 1.0.1 `annotations`, `ast`, and `global-ast` modules.
`klum-ast-test-support:4.0.1` is absent.

Every recorded generated-mirror input check is `false`: the IDE-only mirror is
absent from main Java and Groovy sources, Groovy compilation sources and
classpath, Javadoc sources, JAR sources, and main/test runtime classpaths. The
consumer also applies no publication plugin.

## Remaining horizon

The retained 4.0.x public baseline is now 4.0.1. Binding the unchanged fixture
to one complete staged candidate product remains a future SC-2 gate. Remote
bootstrap decisions, workflow/release gates, Layer 3 changes, and product
repository changes remain outside this validation.
